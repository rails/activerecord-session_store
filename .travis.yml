language: ruby
sudo: false

rvm:
  - 1.9.3
  - 2.0
  - 2.1
  - 2.2
  - ruby-head

gemfile:
  - gemfiles/edge.gemfile

notifications:
  email: false

before_install:
  - gem install bundler

matrix:
  fast_finish: true
  allow_failures:
    - rvm: ruby-head
    - gemfile: gemfiles/edge.gemfile
